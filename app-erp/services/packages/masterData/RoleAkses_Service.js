'use strict';

angular.module('app.erpUtils').factory('RoleAkses_Service', [
    'BackEndService',
    'DTService',
    function (BackEndService, DTService) {
        var service = {};

        return service;
    },
]);
